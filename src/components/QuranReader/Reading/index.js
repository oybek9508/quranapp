import React from "react";

const Reading = ({ initialData, resourceId }) => {
  console.log("initialData", initialData);
  console.log("resourceId", resourceId);
  return <div>Rading</div>;
};

export default Reading;
